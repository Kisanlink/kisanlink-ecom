<!------------------------------------------------------------------------------------
   Add Rules to this file or a short description and have Kiro refine them for you:
------------------------------------------------------------------------------------->

Always use a seperate folder for all the tests and ensure that the subfolder structure inside the tests folder replicates the folder structure of the entire package so that we can add relevant tests to the relacvant folder inside the tests.

also split the tests into actual tests and test data. create a data folder inside the tests for consolidated test data.

dont rerun tests unless abosolutely necessary

ensure that overall coverage is 90% and branch coverage is 80%
